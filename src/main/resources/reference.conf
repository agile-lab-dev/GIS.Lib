{
  graph {
    graph_location = "/"
    contraction_hierarchies_enabled = false
    elevation_enabled = true
    map_matching_algorithm = "dijkstrabi"
    measurement_error_sigma = 50
  }
  osm {

    filter_empty_streets = false
    road_tol_meters = 100.0
    address_tol_meters = 20.0
    vehicle = "car"

    index {
        input_paths = [""]

        boundary {
            administrative: {
        ;        value: 4
        ;        level: 2
             }
        }
        path {
        }
    }
  }
}